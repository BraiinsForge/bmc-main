# Copyright (C) 2025  Braiins Systems s.r.o.

stages:
  - essentials
  - dependencies
  - check
  - apps

##################################
######## Essentials Stage ########
##################################
variables:
  CI_TOOLS_REV: 7f720811d45114a2acef98c0b17b8037ecc57f72 # This is not accessible inside "include"
include:
  - project: 'nix/ci-tools'
    ref: 7f720811d45114a2acef98c0b17b8037ecc57f72
    file:
      - '.essentials.yml'

####################################
######## Dependencies Stage ########
####################################

workspace-deps:
  extends: .nix
  stage: dependencies
  needs: []
  script:
    - nix build .#workspace-deps --no-link

#############################
######## Check Stage ########
#############################

build:
  extends: .nix
  stage: check
  needs:
    - workspace-deps
  script:
    - nix build .#build --no-link

clippy:
  extends: .nix
  stage: check
  needs:
    - workspace-deps
  script:
    - nix build .#clippy --no-link

test:
  extends: .nix
  stage: check
  needs:
    - workspace-deps
  script:
    - nix build .#nextest --no-link

############################
######## Apps Stage ########
############################

.app: &app
  extends: [.nix, .artifacts]
  stage: apps
  needs: []
  when: manual

bmc-mock:
  extends: .app
  script:
    - nix build .#bmc-mock

bmc-openwrt:
  extends: .app
  script:
    - nix build .#bmc-openwrt-armv7-release

web-assets:
  extends: .app
  script:
    - nix build .#web-assets
