# Copyright (C) 2025  Braiins Systems s.r.o.

stages:
  - essentials
  - dependencies
  - check
  - apps

##################################
######## Essentials Stage ########
##################################
variables:
  CI_TOOLS_REV: c5e3f70adc34fdeba3a5fcef7106db474be443a8 # This is not accessible inside "include"
include:
  - project: "nix/ci-tools"
    ref: c5e3f70adc34fdeba3a5fcef7106db474be443a8
    file:
      - ".essentials.yml"
      - ".dependencies.yml"
      - ".check.yml"

verify-tooling-crates:
  extends: .nix
  stage: essentials
  script:
    - nix develop .#fast --command ./scripts/verify_crate_hash.sh tooling

############################
######## Apps Stage ########
############################

.app: &app
  extends: [.nix, .artifacts]
  stage: apps
  needs: []
  when: manual

bmc-mock:
  extends: .app
  script:
    - nix build .#bmc-mock

bmc-openwrt:
  extends: .app
  script:
    - nix build .#bmc-openwrt-armv7-release

web-assets:
  extends: .app
  script:
    - nix build .#web-assets
