[package]
name = "bmc-kobject"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
anyhow.workspace = true

[features]
# Exposes `test_support` to downstream crates' tests.
test-support = []

[lints]
workspace = true
