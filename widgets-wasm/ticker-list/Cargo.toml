[package]
name = "ticker-list"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["cdylib", "lib"]

[features]
scene = []

[dependencies]
bmc-wasm-sdk = { path = "../../bmc-wasm-runtime/sdk" }
prices = { path = "../lib/prices" }

[lints]
workspace = true
