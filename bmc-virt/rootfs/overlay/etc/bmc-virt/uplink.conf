# Fake upstream AP credentials — templated at deploy time.
# Used by 95-bmc-virt-wifi to re-apply radio1 config after bmc-openwrt's
# wifi-detect wipes /etc/config/wireless on startup.
UPLINK_SSID='@@UPLINK_SSID@@'
UPLINK_KEY='@@UPLINK_KEY@@'
