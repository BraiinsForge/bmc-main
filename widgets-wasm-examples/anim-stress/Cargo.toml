[package]
name = "anim-stress"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[package.metadata.nix]
include = ["assets/**"]

[lib]
crate-type = ["cdylib"]

[dependencies]
bmc-wasm-sdk = { path = "../../bmc-wasm-runtime/sdk" }

[lints]
workspace = true
