[package]
name = "bmc-wasm-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
bmc-wasm-protocol = { path = "protocol" }
chrono.workspace = true
tracing.workspace = true

# WASM runtime
wasmi.workspace = true

# Rendering
cosmic-text.workspace = true
femtovg.workspace = true
glow.workspace = true

# Layout
taffy.workspace = true

# Testbed
glutin = { workspace = true, optional = true }
glutin-winit = { workspace = true, optional = true }
notify = { workspace = true, optional = true }
raw-window-handle = { workspace = true, optional = true }
winit = { workspace = true, optional = true }

[features]
default = []
testbed = ["glutin", "glutin-winit", "notify", "raw-window-handle", "winit"]

[[bin]]
name = "testbed"
path = "src/bin/testbed.rs"
required-features = ["testbed"]

[lints]
workspace = true
