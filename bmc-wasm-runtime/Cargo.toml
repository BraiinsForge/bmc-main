[package]
name = "bmc-wasm-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
bmc-wasm-protocol = { path = "protocol" }
chrono.workspace = true
chrono-tz.workspace = true
formato.workspace = true
tracing.workspace = true

# WASM runtime
wasmi.workspace = true

# Rendering
cosmic-text.workspace = true
femtovg.workspace = true
glow.workspace = true
image.workspace = true
imgref.workspace = true
rgb.workspace = true

# Layout
taffy.workspace = true

# Network + JSON/XML (for host-side fetch and parsing)
mdns-sd.workspace = true
roxmltree.workspace = true
rrule.workspace = true
rustls.workspace = true
serde_json.workspace = true
tungstenite.workspace = true
ureq.workspace = true

# Host binaries (GL windowing)
glutin = { workspace = true, optional = true }
glutin-winit = { workspace = true, optional = true }
raw-window-handle = { workspace = true, optional = true }
tracing-subscriber = { workspace = true, optional = true }
winit = { workspace = true, optional = true }

# Testbed only
notify = { workspace = true, optional = true }

# Fixtures (shared between capture and testbed)
flate2 = { workspace = true, optional = true }
serde = { workspace = true, optional = true }

# Capture only
askama = { workspace = true, optional = true }
base64 = { workspace = true, optional = true }
clap = { workspace = true, optional = true }
libc = { workspace = true, optional = true }
owo-colors = { workspace = true, optional = true }
rayon = { workspace = true, optional = true }
sevenz-rust2 = { workspace = true, optional = true }
tempfile = { workspace = true, optional = true }
toml = { workspace = true, optional = true }

[features]
default = []
perf-overlay = []
host = ["glutin", "glutin-winit", "raw-window-handle", "tracing-subscriber", "winit"]
fixtures = ["host", "base64", "flate2", "owo-colors", "serde", "toml"]
testbed = ["host", "perf-overlay", "notify", "fixtures"]
capture = ["host", "askama", "base64", "clap", "libc", "fixtures", "rayon", "sevenz-rust2", "tempfile"]

[[bin]]
name = "testbed"
path = "src/bin/testbed.rs"
required-features = ["testbed"]

[[bin]]
name = "capture"
path = "src/bin/capture/main.rs"
required-features = ["capture"]

[build-dependencies]
bmc-icon-compiler = { path = "icon-compiler" }
bmc-wasm-protocol = { path = "protocol" }

[lints]
workspace = true
