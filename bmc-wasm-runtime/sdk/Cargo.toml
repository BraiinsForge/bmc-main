[package]
name = "bmc-wasm-sdk"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
keyframe = { version = "1.1", default-features = false }
libm = "0.2"

# Keep this crate out of the main workspace
[workspace]
