# Future Improvements

## Binary Compatibility

- Add SDK version detection to host runtime
- Check for binary format compatibility at widget load time
- Notify widget authors when SDK version mismatch requires recompile

## API Enhancements

- Text measurement callback (widget needs text size for some layouts)
- Host-side formatting for dynamic strings
- More draw primitives (circles, lines, etc.)

## Performance (if needed)

- Profile tree serialization overhead
- Pool/reuse tree buffers across frames

